# OpenMemory Plus v2.0 路线图

**愿景**: 成为 AI Agent 生态中最好的开源记忆层  
**定位**: 独立发展，专注记忆能力，可被任何框架集成  
**日期**: 2026-02-02

---

## 🎯 核心价值主张

> "让任何 AI Agent 在 5 分钟内获得持久记忆能力"

| 差异化优势 | 说明 |
|-----------|------|
| **双层架构** | 项目级 (.memory/) + 用户级 (openmemory) 分离 |
| **多 CLI 共享** | Augment, Claude, Cursor 共享同一记忆层 |
| **智能分类** | 自动判断信息存储位置 |
| **轻量专注** | 不是全栈框架，只做记忆这一件事 |

---

## 📅 Phase 1: 快速安装体验 (2 周)

**目标**: `npx openmemory-plus init` 一键完成

### 1.1 发布到 npm

```bash
# 用户体验
npx openmemory-plus@latest init
# 或全局安装
npm install -g openmemory-plus
omp init
```

**任务清单**:
- [ ] 完善 package.json 元数据
- [ ] 添加 `files` 字段包含 templates/
- [ ] 发布到 npm registry
- [ ] 创建 GitHub Release

### 1.2 模板系统完善

**当前问题**: IDE 模板不完整

**解决方案**: 已有 `templates/shared/` 结构，需确保所有 IDE 入口文件完整

```
templates/
├── shared/           ✅ 已完成
│   ├── commands/     ✅ 6 个命令
│   ├── skills/       ✅ memory-extraction
│   └── rules/        ✅ classification.md
├── augment/          ⚠️ 需要入口文件
├── claude/           ⚠️ 需要入口文件
├── cursor/           ⚠️ 需要入口文件
└── common/           ⚠️ 需要入口文件
```

### 1.3 MCP 配置自动生成

```bash
openmemory-plus init --generate-mcp
# 自动生成 MCP 配置片段，用户复制到 IDE 配置
```

---

## 📅 Phase 2: 记忆能力增强 (4 周)

**目标**: 超越简单的 key-value 存储

### 2.1 Graph Memory (图记忆)

```yaml
# .memory/graph.yaml
entities:
  - id: "project-api"
    type: "service"
    name: "API 服务"
    relations:
      - target: "database-postgres"
        type: "depends_on"
      - target: "user-preference-dark-mode"
        type: "configured_by"
```

**能力**:
- 实体关系图谱
- 关联查询 ("与 API 相关的所有配置")
- 知识推理

### 2.2 时间衰减 (Memory Decay)

```typescript
interface Memory {
  content: string;
  createdAt: Date;
  lastAccessedAt: Date;
  accessCount: number;
  decayScore: number;  // 0-1, 越低越可能被遗忘
}
```

**规则**:
- 30 天未访问 → 降低优先级
- 90 天未访问 → 标记为可清理
- 高频访问 → 提升为核心记忆

### 2.3 冲突检测与解决

```bash
/mem sync
# 检测 .memory/ 和 openmemory 的冲突
# 例如: 项目配置说 "使用 PostgreSQL"，但用户记忆说 "偏好 MySQL"
```

---

## 📅 Phase 3: 多 Agent 共享 (6 周)

**目标**: 解决你提出的核心问题 - 多 CLI 记忆共享

### 3.1 跨 CLI 同步

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Augment   │     │   Claude    │     │   Cursor    │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │
       └───────────────────┼───────────────────┘
                           │
                    ┌──────▼──────┐
                    │  OpenMemory │
                    │   (Qdrant)  │
                    └─────────────┘
```

**实现**: 所有 CLI 通过 MCP 协议访问同一个 openmemory 实例

### 3.2 项目记忆同步

```bash
# 在项目 A 中
openmemory-plus export --to project-a-memory.json

# 在项目 B 中
openmemory-plus import --from project-a-memory.json --merge
```

### 3.3 团队记忆 (可选)

```yaml
# .memory/team.yaml
team:
  sync_endpoint: "https://team-memory.example.com"
  members:
    - user_id: "alice"
      role: "admin"
    - user_id: "bob"
      role: "member"
```

---

## 🛠️ 技术实现优先级

| 优先级 | 功能 | 工作量 | 影响 |
|--------|------|--------|------|
| P0 | npm 发布 | 1 天 | 用户可 npx 安装 |
| P0 | 模板入口文件 | 1 天 | 所有 IDE 可用 |
| P1 | MCP 配置生成 | 2 天 | 降低配置门槛 |
| P1 | 时间衰减 | 3 天 | 智能遗忘 |
| P2 | Graph Memory | 1 周 | 关系查询 |
| P2 | 冲突检测 | 3 天 | 数据一致性 |
| P3 | 导入导出 | 2 天 | 项目迁移 |
| P3 | 团队同步 | 2 周 | 企业场景 |

---

## 📊 成功指标

| 指标 | Phase 1 | Phase 2 | Phase 3 |
|------|---------|---------|---------|
| npm 周下载量 | 100+ | 500+ | 2000+ |
| GitHub Stars | 50+ | 200+ | 1000+ |
| 支持 IDE 数量 | 4 | 4 | 6+ |
| 记忆类型 | 2 (项目/用户) | 4 (+图/时序) | 5 (+团队) |

---

## 🚀 立即行动

**本周目标**:
1. 发布 npm 包
2. 完善 IDE 模板入口
3. 更新 README 安装说明

**下周目标**:
1. MCP 配置生成
2. 时间衰减基础实现
3. 收集用户反馈

